import React from 'react';
import { processSiteFile, processMetricsFile } from '../../services/api';

export const FileUpload = ({ onSiteUpload, onMetricsUpload }) => {
  // File upload implementation
}; 